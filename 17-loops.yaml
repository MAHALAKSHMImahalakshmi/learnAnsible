- name: loops
  hosts: local
  connection: local
  tasks:
    - name: print names
      ansible.builtin.debug:
        msg: "hi , {{ iteam }}"
      loop:
        - suresh
        - ramesh
        - joseph
        - raheem
